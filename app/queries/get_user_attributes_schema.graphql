query GetUserAttributesSchema {
  schema {
    userSchema {
      attributes {
        name
        aliases
        attributeType
        isList
        isVisible
        isEditable
        isHardcoded
        isReadonly
      }
    }
  }
}
