query GetUserDetails($id: String!) {
  user(userId: $id) {
    id
    email
    avatar
    displayName
    creationDate
    uuid
    groups {
      id
      displayName
    }
    attributes {
      schema {
        name
      }
      value
    }
  }
  schema {
    userSchema {
      attributes {
        name
        aliases
        attributeType
        isList
        isVisible
        isEditable
        isHardcoded
        isReadonly
      }
    }
  }
}
