query GetGroupAttributesSchema {
  schema {
    groupSchema {
      attributes {
        name
        aliases
        attributeType
        isList
        isVisible
        isEditable      # ← added for full parity with user schema
        isHardcoded
        isReadonly
      }
    }
  }
}
