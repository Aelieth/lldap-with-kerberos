query GetGroupAttributesSchema {
  schema {
    groupSchema {
      attributes {
        name
        aliases
        attributeType
        isList
        isVisible
        isEditable
        isHardcoded
        isReadonly
      }
    }
  }
}
