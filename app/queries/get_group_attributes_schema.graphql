query GetGroupAttributesSchema {
  schema {
    groupSchema {
      attributes {
        name
        aliases
        attributeType
        isList
        isVisible
        isHardcoded
        isReadonly
      }
    }
  }
}
